
pipeline:
  build:
    image: node:8.11.3-alpine
    commands:
      - yarn install
  slack:
    image: plugins/slack
    webhook: https://hooks.slack.com/services/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
    channel: "#ibmcloud"
    template: >
      {{#success build.status}}
        build {{build.number}} succeeded. Good job 目前運行非常正常nice!.
      {{else}}
        build {{build.number}} failed. Fix me please 現在build失敗.
      {{/success}}